// 日期计算的 UI 样式常量。Button / Dropdown / Checkbox / Input 等公共组件在 $lib/ui，
// 工具条与分段按钮常量在 $lib/ui/styles.ts，这里只留日期表单的小标签与结果小卡。

/** 表单字段的小标签 */
export const LABEL = 'mb-1 block text-xs font-medium text-gray-600';

/** 结果小卡（相差几周 / 几个月这类次级指标） */
export const STAT_CARD = 'rounded-lg border border-gray-200 bg-gray-50 p-3';
